# Synced mirror（勿手工改）

本目录由 **`npm run sync:harness-skill`** 从仓库 **`docs/`**、**`harness/`** 覆盖生成。

- 权威内容在仓库根下对应路径；此处仅供 Cursor Skill 打包阅读。
- 编辑请改 **`docs/`** 或 **`harness/`**，保存后若已启用 `after-file-sync-harness-skill` hook 会自动再同步。

生成时间（本地）：2026-05-11T16:48:46.914Z
