/** 与后端 `appendFeatureJobRun` 使用的 `save-session` featureId 一致 */
export const SAVE_SESSION_FEATURE_ID = "save-session";

/** 晓风低价好物 · 与 `src/features/xf-ali-find-low-goods` 及 UI 进度解析一致 */
export const XF_LOW_GOODS_FEATURE_ID = "xf-ali-find-low-goods";
